apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    appname: busybox_app4-75
  name: busybox-75
spec:
  selector:
    matchLabels:
      workload: busybox-75
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        workload: busybox-75
    spec:
      containers:
      - command:
        - sh
        - -c
        - while true; do echo $(date) | tee -a /mnt/test/outfile; sync; sleep 5; done
        image: quay.io/vcppds7878/busybox:latest
        imagePullPolicy: IfNotPresent
        name: busybox
        volumeMounts:
        - mountPath: /mnt/test
          name: mypvc
      volumes:
      - name: mypvc
        persistentVolumeClaim:
          claimName: busybox-pvc-75
          readOnly: false
