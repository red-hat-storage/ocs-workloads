apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    appname: busybox_app3-58
  name: busybox-58
spec:
  selector:
    matchLabels:
      workload: busybox-58
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        workload: busybox-58
        workloadpattern: simple_io
    spec:
      containers:
      - command:
        - sh
        - -c
        - while true; do echo $(date) | tee -a /mnt/test/outfile; sync; sleep 0; done
        image: quay.io/ocsci/busybox:latest
        imagePullPolicy: IfNotPresent
        name: busybox
        volumeMounts:
        - mountPath: /mnt/test
          name: mypvc
      volumes:
      - name: mypvc
        persistentVolumeClaim:
          claimName: busybox-pvc-58
          readOnly: false
