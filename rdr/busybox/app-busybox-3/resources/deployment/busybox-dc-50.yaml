apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    appname: busybox_app3-50
  name: busybox-50
spec:
  selector:
    matchLabels:
      workload: busybox-50
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        workload: busybox-50
    spec:
      containers:
      - command:
        - sh
        - -c
        - while true; do echo $(date) | tee -a /mnt/test/outfile; sync; sleep 3; done
        image: quay.io/vcppds7878/busybox:latest
        imagePullPolicy: IfNotPresent
        name: busybox
        volumeMounts:
        - mountPath: /mnt/test
          name: mypvc
      volumes:
      - name: mypvc
        persistentVolumeClaim:
          claimName: busybox-pvc-50
          readOnly: false
